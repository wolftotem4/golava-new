package app

import (
{{.packages}}
)

type App struct {
	golava.App

	Loggers *Loggers
	DB {{.dbType}}{{range .exts}}
	{{.Name}} {{.Declare}}{{end}}
}

func (a *App) Base() *golava.App {
	return &a.App
}
